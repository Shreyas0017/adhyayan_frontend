version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version || true
        - npm ci
    build:
      commands:
        - npm run build
        - echo "---- ls client/.next ----"
        - ls -la .next || true
  artifacts:
    # Amplify will look for the .next folder at repo-root/client/.next
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
